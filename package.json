{
  "name": "theta-jewel-analytics-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:data": "node scripts/build-dashboard-data.mjs",
    "build": "npm run build:data && node scripts/build-static-site.mjs",
    "dev": "npm run build && npx serve dist",
    "start": "npx serve dist",
    "refresh": "node scripts/refresh-theta-app.mjs"
  },
  "dependencies": {},
  "devDependencies": {}
}
